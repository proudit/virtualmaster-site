dependencies:
    override:
        - bundle install
        - sudo pip install awscli

deployment:
    aws:
    branch: master
        commands:
        - aws s3 sync html/ s3://virtualmaster.jp/ --delete
